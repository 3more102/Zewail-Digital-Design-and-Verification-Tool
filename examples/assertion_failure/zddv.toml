[project]
name = "assertion-failure"

[simulator]
backend = "verilator"
top = "tb_assertion_failure"

[sources]
rtl = []
tb = ["tb/*.sv"]

[run]
build_dir = ".zddv/build"
run_dir = ".zddv/runs"
waveform = false
coverage = false
